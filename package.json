{
  "name": "@brightspace-ui/testing",
  "description": "Utilities for testing front-end components and applications",
  "type": "module",
  "version": "0.1.1",
  "repository": "https://github.com/BrightspaceUI/testing.git",
  "scripts": {
    "lint": "eslint . --ext .js",
    "test": "npm run lint && npm run test:server",
    "test:headless": "web-test-runner --files \"./test/browser/**/*.test.js\" --node-resolve --playwright",
    "test:server": "mocha ./test/server/**/*.test.js"
  },
  "author": "D2L Corporation",
  "license": "Apache-2.0",
  "devDependencies": {
    "chai": "^4",
    "eslint": "^8",
    "eslint-config-brightspace": "^0.23",
    "mocha": "^10",
    "sinon": "^15"
  },
  "exports": "./src/index.js",
  "files": [
    "/src"
  ],
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@open-wc/testing": "^3",
    "@web/test-runner": "^0.16",
    "@web/test-runner-playwright": "^0.10"
  }
}
